CREATE TABLE movie_entity(
    movieId INTEGER NOT NULL UNIQUE,
    title TEXT NOT NULL,
    overview TEXT NOT NULL,
    posterPath TEXT,
    backdropPath TEXT,
    page INTEGER NOT NULL,
    sortOrder INTEGER NOT NULL
);

selectAllMovies:
SELECT * FROM movie_entity ORDER BY sortOrder ASC ;

